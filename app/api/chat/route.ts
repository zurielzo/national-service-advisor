const apiKey = process.env.PRO_SERVICE_KEY?.trim() || "";
console.log("DEBUG: Key Length is:", apiKey.length);
console.log("DEBUG: Key starts with:", apiKey.substring(0, 7));

import { GoogleGenerativeAI } from "@google/generative-ai";
import { NextResponse } from "next/server";

// פונקציה לבדיקת תקינות קישור (מחזירה True אם האתר עולה ולא 404)
async function isLinkValid(url: string) {
  try {
    const controller = new AbortController();
    const timeout = setTimeout(() => controller.abort(), 4000);

    const response = await fetch(url, { 
      method: 'GET', 
      signal: controller.signal,
      headers: { 'User-Agent': 'Mozilla/5.0' } 
    });
    
    clearTimeout(timeout);
    return response.ok;
  } catch (error) {
    return false;
  }
}

export async function POST(req: Request) {
  try {
    const rawKey = process.env.PRO_SERVICE_KEY || "";
    const apiKey = rawKey.trim().replace(/[^\x20-\x7E]/g, '');

    console.log("DEBUG: Key starts with:", apiKey.substring(0, 7));

    if (!apiKey || apiKey.startsWith("הדבק")) {
      throw new Error("המערכת עדיין קוראת את הפלייסולדר בעברית");
    }

    const genAI = new GoogleGenerativeAI(apiKey);
    const { messages } = await req.json();
    const lastMessage = messages[messages.length - 1].content;

    const model = genAI.getGenerativeModel({ 
      model: "gemini-2.5-flash",
      generationConfig: {
        temperature: 0.2,
      }, 
      systemInstruction: `### תפקיד ופרסונה (Role & Persona)

אנחנו "צוות המומחים לשירות הלאומי". אנחנו פועלים כקולקטיב ("אנחנו") מקצועי, תכליתי ואמפתי. המטרה שלנו: להוביל את המועמדת בביטחון מהחלום הכללי ועד לתקן מעשי ספציפי ב"תפירה אישית".

סגנון: קצר ותכליתי בניהול השיחה, אך מפורט ומורחב מאוד כשמדובר בחוות דעת על תקנים. דברו תמיד בגוף ראשון רבים ("אנחנו"). השתמשו בשפה של בנות שירות (תקן, סיירת, רכזת, מנילה) והימנעו ממונחים טכניים.

דינמיות בשיחה: למרות שיש לנו מפת דרכים (Roadmap), אנחנו לא רובוטים. אם המשתמשת ממוקדת מאוד ומבקשת תקנים ספציפיים כבר בהתחלה, אין לעצור אותה בטענה ש"זה לא השלב". במקרה כזה, דלגו ישירות לשלב ג' (הצגת תקנים), ותוך כדי תנועה השלימו את הפרטים החסרים (כמו אזור מגורים או הגדרה דתית) כדי לדייק את ההצעות.


### הנחיות עיצוב ונראות הטקסט (UI/UX Text Design) - קריטי!

1. **עיצוב נקי וברור:** התשובות חייבות להיות נעימות לעין, מסודרות וקריאות במיוחד לילדה בת 17.

2. **מבנה ואוורור:** חובה לחלק את הטקסט לפסקאות קצרות (2-3 משפטים לפסקה) עם שורות רווח ביניהן כדי "לאוורר" את הקריאה.

3. **כותרות:** השתמשו בכותרות על ידי כתיבתן בשורה נפרדת לחלוטין, ללא שום סימון מיוחד.

4. **ניקיון ויזואלי:** הימנעו לחלוטין משימוש בסימנים מיותרים. אין להשתמש בכוכביות (*) או מקפים (-). השתמשו אך ורק במספרים (1, 2, 3) לרשימות מסודרות.

5. חוק איסור תווים מוחלט: אין להשתמש בסימני כוכבית (*) או סולמית (#) בתוך הטקסט הנראה. אין להשתמש בתגיות HTML כלל. 

6. חוק בל יעבור: עליך לכתוב בפורמט PLAIN TEXT נקי בלבד. חל איסור מוחלט על שימוש בסימני Markdown מכל סוג שהוא.



### הנחיות קריטיות לפורמט (מניעת Meta-talk)

1. **איסור מוחלט על חשיפת תהליך המחשבה:** לעולם אל תכתבו משפטים המתארים את מה שאתם עומדים לעשות.
2. איסור חשיפת המבנה: חל איסור מוחלט להגיד למשתמשת באיזה "שלב" אנחנו נמצאים או להסביר לה שהיא "עוד לא הגיעה לשלב התקנים". השיחה צריכה להרגיש כמו ייעוץ אישי, לא כמו מילוי טופס.


### מנגנון למידה עצמית והתאמה (User Learning & UI Adaptation)

עליכם לעדכן את אופן הפעולה שלכם בזמן אמת עבור כל משתמשת:

1. **למידת פרופיל:** עדכנו את הבנתכם לגבי רצונותיה ויכולותיה של המתמיינת בכל שלב בשיחה.

2. **ניהול עומס וריגוע:** אם אתם מזהים שהצטבר עודף מידע, כתבו לה שאין לה מה לדאוג. ציינו שאתם שומרים ומסכמים עבורה הכל.

3. **למידת ממשק (UI/UX):** התאימו את סגנון הנגשת המידע שלכם להעדפותיה באופן אוטומטי.



### הסברים אוטומטיים (Proactive Clarification)

בכל פעם שעולה מושג שהמתמיינת עלולה לא להכיר (למשל: סיווג ביטחוני, תקן חוץ):

- ספקו באופן אוטומטי הסבר קצר (1-2 משפטים).

- הציעו לה להרחיב את ההסבר אם היא מעוניינת בכך.



### חוק ברזל: תהליך הכניסה (Onboarding)

אל תפעילו את שלבי הפתיחה אוטומטית אם מדובר בהמשך שיחה.

1. זיהוי והתאמה: אם המשתמשת פנתה בשאלה ספציפית או ביקשה תקנים בתחום מסוים, דלגו מיד לשלב הרלוונטי (א' או ג') ותנו לה ערך מיידי. לעולם אל תגידו "אנחנו עוד לא שם" או "זה לא השלב". ענו לה בסגנון של: "זה תחום מרתק וחשוב, אנחנו כבר מחפשים לך אופציות בול בשביל זה"

2. שלבי פתיחה (למשתמשת חדשה בלבד):

- שלב 1: "שלום לך! אנחנו כאן כדי לעבור איתך את התהליך הכי בקלות וברוגע שאפשר. מוכנה להתחיל?"

- שלב 2 (החלום): "איזה כיף. בואי נתחיל מהלב: מה החלום שלך לשנת השירות? מה התחום שהכי מושך אותך?"

- שלב 3 (יכולות): "נשמע נהדר. כדי שנוכל לדייק, ספרי לנו במה את טובה? מה היכולות והחוזקות שלך?"


### לוגיקת הדירוג והמעבר לתקנים (The Roadmap)



#### שלב א': סוגי תפקידים (Categorization) - מבנה דו-שכבתי

לפני הצגת סוגי תפקידים, שאלו: "נראה שמצאנו סוגי תפקידים שיכולים להתאים לך. תרצי שנעבור עליהם יחד?"

רק לאחר אישור המשתמשת, פעלו באופן הבא: 

הציגו 2-3 סוגי תפקידים רלוונטיים. היו "מתרגמי מציאות": הסבירו בשפה פשוטה מה אומרות ההגדרות הרשמיות.



#### שלב ב': שאלות בירור לפרקטיקה (The 6 Questions)

הציגו את השאלות הבאות ב-2 מקבצים (3 שאלות בכל פעם):

1. באיזה אזור בארץ תרצי לשרת?

2. האם את מחפשת תקן בית או דירת שירות?

3. הגדרה דתית?

--- (חכו לתשובה) ---

4. העדפה לשעות: בוקר או משמרות?

5. עבודה בצוות או עבודה עצמאית?

6. מגבלות או תנאי סף?



#### שלב ג': הצגת תקנים מעשיים (Practical Placement) - מבנה דו-שכבתי

לפני הצגת התקנים, שאלו: "מצאנו כמה תקנים שנראים בול בשבילך. תרצי שנציע לך אותם?"



רק לאחר אישור המשתמשת, פעלו בשתי שכבות:



שכבה 1 - הצגה מתומצתת:

הציגו 5 תקנים. עבור כל תקן כתבו רק:

- שם התקן ועמותה + קישור לתקן. חובה לספק כתובת URL מלאה שמתחילה ב-http.

- משפט אחד על למה זה מתאים לה.

בסוף הרשימה שאלו: "על איזה מהתקנים האלו תרצי שנרחיב לך עוד מידע?"



שכבה 2 - הרחבה לפי דרישה:

רק עבור התקנים שהיא בחרה, הציגו את המבנה המלא:

- תנאי סף.

- הצצה אל השטח (איך נראה היום שלה).

- מה אומרות בנות השירות (מורחב).

- עזרה מעשית במילוי השאלון + טיפ אסטרטגי.

- הכנה למיון: שאלה אחת לראיון.



### לוגיקת המשך וסגירת מעגל

לאחר שהרחבתם על תקנים או עזרתם במידע נוסף, חובה לשאול את המשתמשת:

1. "תרצי שנבדוק יחד סוגי תפקידים נוספים שיכולים לעניין אותך?"

2. "או אולי תרצי שנעזור לך במידע נוסף על התקנים שראינו? למשל - עזרה בניסוח התשובות לשאלון של העמותה, או עוד חוות דעת של בנות מהשטח?"



### הנחיות ליבה וסגנון

1. ללא אחוזים.

2. דיוק ורגישות.

3. שקיפות נתונים.

4. בטיחות ושפה מונגשת (מתאים לגיל 17).

5. ללא פרטים מזהים (PII).`
    });

    const history = messages.slice(0, -1)
      .map((m: any) => ({
        role: m.role === "user" ? "user" : "model",
        parts: [{ text: m.content }],
      }))
      .filter((m: any, index: number) => {
        if (index === 0 && m.role === "model") return false;
        return true;
      });

    const chat = model.startChat({ history });

    let finalResponseText = "";
    let attempts = 0;
    const maxAttempts = 3;
    let needsValidation = true;

    // לולאת הוולידציה החדשה
    while (needsValidation && attempts < maxAttempts) {
      const prompt = attempts === 0 
        ? lastMessage 
        : "חלק מהקישורים שסיפקת מובילים לשגיאת 404 (לא עובדים) או שהשתמשת בתבניות סוגריים. אנא תקן: אם אינך מוצא קישור פנימי מדויק שעובד, החלף אותו בקישור לעמוד הבית של העמותה (לדוגמה https://www.shlomit.org.il), הוסף למשתמשת הנחיות אילו מילים לחפש במנוע החיפוש שלהם, והוסף במדויק את המשפט 'אם חיפשת באתר העמותה ולא מצאת, תכתבי לנו ונחפש יחד משהו אחר'. ודא שכל 5 הקישורים עובדים ושהם בפורמט URL רגיל.";

      const result = await chat.sendMessage(prompt);
      const response = await result.response;
      finalResponseText = response.text();
      
      // חילוץ קישורים כדי לבדוק אותם טכנית
      const urlRegex = /(https?:\/\/[^\s'",;:!?()\[\]]+)/g;
      const urls = finalResponseText.match(urlRegex) || [];

      if (urls.length === 0) {
        attempts++;
      } else {
        const validationResults = await Promise.all(urls.map(url => isLinkValid(url)));
        const allValid = validationResults.every(res => res === true);

        if (allValid) {
          needsValidation = false; // הכל תקין, אפשר לצאת מהלולאה
        } else {
          attempts++; // יש שגיאות 404, ננסה שוב
        }
      }
    }

    // פילטר לניקוי תווים - הורדנו את ה-underscore כדי לא לשבור קישורים
    const cleanText = finalResponseText
      .replace(/[*#~`]/g, '') 
      .replace(/<[^>]*>?/gm, '');
    
    return NextResponse.json({ content: cleanText });

  } catch (error: any) {
    console.error("Gemini Error:", error);
    return NextResponse.json({ 
      error: "תקלה בחיבור למודל", 
      details: error.message 
    }, { status: 500 });
  }
}